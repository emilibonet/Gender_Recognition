# !!! Conda environment is required !!!

# Install pytorch with torchvision with pip. Cannot be installed in conda environment due to conflicts with opencv.
# pip3 install torch==1.11.0+cu113 torchvision==0.12.0+cu113 -f https://download.pytorch.org/whl/cu113/torch_stable.html

# Libraries for baseline
pip3 install cmake
pip3 install face-recognition

pip3 install deepface